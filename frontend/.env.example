# Copy to .env.local for local development (not committed to git).
# In production (Vercel, etc.), set this as a build-time environment
# variable in the hosting platform's dashboard.

# Full URL of the deployed backend API, no trailing slash.
# Example: https://anvara-health-api.onrender.com
REACT_APP_API_URL=http://localhost:8000
